import { ApiClient } from "..";
type Permission = {
    id: string;
    object: string;
    created: number;
    allow_create_engine: boolean;
    allow_sampling: boolean;
    allow_logprobs: boolean;
    allow_search_indices: boolean;
    allow_view: boolean;
    allow_fine_tuning: boolean;
    organization: string;
    group: string | null;
    is_blocking: boolean;
};
type Model = {
    id: string;
    object: "model";
    owned_by: string;
    permission: Permission[];
    root: string;
    parent: string | null;
};
type DeletedModel = {
    id: string;
    object: "model";
    deleted: boolean;
};
export declare function listModels(client: ApiClient): () => Promise<Model[]>;
export declare function retrieveModel(client: ApiClient): (id: string) => Promise<Model>;
export declare function deleteModel(client: ApiClient): (id: string) => Promise<DeletedModel>;
export {};
