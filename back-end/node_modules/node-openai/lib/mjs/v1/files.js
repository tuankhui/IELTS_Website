import { readFile } from "../request/index.js";
export function listFiles(client) {
    return async () => {
        return await client("files", { method: "GET" });
    };
}
export function uploadFile(client) {
    return async (file, purpose) => {
        const form = {
            'purpose': purpose,
            'file': readFile(file)
        };
        return await client("files", { method: "POST", form });
    };
}
export function deleteFile(client) {
    return async (id) => {
        return await client(`files/${id}`, { method: "DELETE" });
    };
}
export function retrieveFile(client) {
    return async (id) => {
        return await client(`files/${id}`, { method: "GET" });
    };
}
export function retrieveFileContent(client) {
    return async (id) => {
        return await client(`files/${id}/content`, { method: "GET" }, true);
    };
}
//# sourceMappingURL=files.js.map