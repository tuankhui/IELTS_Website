export function listModels(client) {
    return async () => {
        const list = await client("models", { method: "GET" });
        return list.data;
    };
}
export function retrieveModel(client) {
    return async (id) => {
        return await client(`models/${id}`, { method: "GET" });
    };
}
export function deleteModel(client) {
    return async (id) => {
        return await client(`models/${id}`, { method: "DELETE" });
    };
}
//# sourceMappingURL=models.js.map