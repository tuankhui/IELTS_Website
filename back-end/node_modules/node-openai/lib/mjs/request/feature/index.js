import { HeadersFeature } from './headers.js';
import { DataFeature } from './data.js';
import { FormFeature } from './form.js';
import { MethodFeature } from './method.js';
import { SignalFeature } from './signal.js';
const features = [
    new HeadersFeature(),
    new DataFeature(),
    new FormFeature(),
    new MethodFeature(),
    new SignalFeature(),
];
export default async function requestFeature(fn) {
    for (const feature of features) {
        await fn(feature);
    }
}
//# sourceMappingURL=index.js.map