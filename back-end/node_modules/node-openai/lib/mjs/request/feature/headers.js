export class HeadersFeature {
    forRequest(init, options) {
        if (init.headers) {
            options.headers = {
                ...options.headers,
                ...init.headers,
            };
        }
    }
    async forRequestClient(init, client) { }
    forFetch(init, options) {
        if (init.headers) {
            options.headers = {
                ...options.headers,
                ...init.headers,
            };
        }
    }
}
//# sourceMappingURL=headers.js.map