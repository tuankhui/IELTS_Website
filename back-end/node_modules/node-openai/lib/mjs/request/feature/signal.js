export class SignalFeature {
    forRequest(init, options) {
        if (init.signal) {
            options.signal = init.signal;
        }
    }
    async forRequestClient(init, client) { }
    forFetch(init, options) {
        if (init.signal) {
            options.signal = init.signal;
        }
    }
}
//# sourceMappingURL=signal.js.map